# 5.Create a module called my_module that defines a function to calculate the square of a number. Import the module into a 
# new program and use the function to calculate the square of a number.

def squre_number(number):
    return number ** 2
